make -Wall -C /lib/modules/`uname -r`/build M=`pwd` modules

sudo insmod helloworld.ko

cat /proc/devices | ag helloworld

sudo mknod /dev/demo c 60 0  // 建立裝置檔案

cat /proc/devices | ag demo

sudo ./test
